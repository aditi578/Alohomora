// Set the value of the PORT variable to 5000
const PORT = 5000;

// Set the value of the URI variable to connect to a MongoDB database
const URI = "mongodb://127.0.0.1:27017/libdb";

// The above code assumes that the MongoDB server is running locally on the default port (27017),
// and the database name is "libdb". Adjust the URI as per your MongoDB configuration.

// Export the PORT and URI variables to be used in other files
module.exports = {
  PORT,
  URI
};
